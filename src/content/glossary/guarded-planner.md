---
term: Guarded planner
product: windchime
definition: >-
  The pattern where a language model fills a validated JSON schema instead of writing code
  directly; a compiler then renders safe output. It keeps generation inspectable and prevents
  the model from ever emitting raw code into a live installation.
related: [strudel]
---
